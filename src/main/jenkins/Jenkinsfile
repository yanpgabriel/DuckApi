pipeline {
    agent any

    tools('Configurando Ferramentas') {
        maven 'Maven Jenkins'
    }

    stages {
        stage('Checkout') {
            steps {
                // Limpa o Workspace
                cleanWs()
                // Baixa o codigo
                git branch: "${GIT_BRANCH}", url: 'https://github.com/yanpgabriel/quarkus-security-jpa.git'
            }
        }
        stage('Build') {
            steps {
                // Roda o maven
                sh "mvn clean package ${MAVEN_ARGS}"
            }
        }
    }
}
